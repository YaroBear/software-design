var MineSweeper = function(){
	//this.Cell = {EXPOSED : false};  //Venkat: not needed yet
};

MineSweeper.prototype.exposeCell = function(row, column){
	//this.Cell.EXPOSED = true; //Venkat: not needed yet
};

MineSweeper.prototype.isCellExposed = function(row, column){
	//return this.Cell.EXPOSED;  //Venkat: not needed yet
	return true;
};